name: CI Debian

on:
  push:
    branches:
      - master
  pull_request:


jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        debian_version: [bullseye, bookworm]
    container:
      image: debian:${{ matrix.debian_version }}

    steps:
    - uses: actions/checkout@v2

    - name: Install dependencies
      run: |
        #!/bin/bash
        apt update && apt install -y cmake build-essential pkg-config libssl-dev protobuf-compiler-grpc libgrpc++-dev && mkdir -p kuka_external_control_sdk/build && mkdir -p kuka_external_control_sdk_examples/build

    - name: Build and install
      run: cmake -DCMAKE_TESTING_ENABLED=1 .. && make install
      working-directory: ./kuka_external_control_sdk/build

    - name: Build and install examples
      run: cmake -DCMAKE_TESTING_ENABLED=1 .. && make
      working-directory: ./kuka_external_control_sdk_examples/build
